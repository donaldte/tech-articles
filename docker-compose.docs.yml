services:
  docs:
    image: tech_articles_local_docs
    container_name: tech_articles_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./tech_articles:/app/tech_articles:z
    ports:
      - '9000:9000'
    command: /start-docs
